# Feature Parity Matrix — Fadhkur (فذكر) vs. Baseline Reference

This document provides a thorough audit of the functional scope and feature parity of **Fadhkur (فذكر)** in comparison to the canonical Quran baseline and reference application.

| Domain (المجال) | Reference Function (وظيفة المرجع) | Fadhkur Implementation (تنفيذ فذكر) | Status | Evidence (الدليل) | Notes (ملاحظات) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Home (الرئيسية)** | Daily reading, featured reciter, prayer times widget, live radio card | Full Arabic RTL dashboard with live player sheet, prayer bar, and fast links; visible mock data replaced with real repositories and persistent user state (Phase-1 audit 2026-09-23) | **PASS** | `apps/mobile/lib/features/home/presentation/home_screen.dart`, `apps/mobile/lib/features/home/data/reading_progress_repository.dart` | Sovereign design tokens applied (`#243B6B`, `#2E9E9E`, `#C77955`, `#F8F6F1`) |
| **Radio (الإذاعة المدارة)** | 24/7 continuous radio streaming | Managed Liquidsoap 2.2 + Icecast cluster (`/live/khashia.mp3`, `/live/murattal.mp3`) with auto failover | **PASS** | `infrastructure/liquidsoap/radio.liq`, `infrastructure/icecast/icecast.xml`, `services/radio-engine/src/index.ts` | EBU R128 (-16 LUFS) normalization, deterministic occurrence IDs, anti-double-play fencing |
| **External Stations (المحطات الخارجية)** | Play public Egyptian/Saudi radio streams | Explicit virtual station isolation model with source playback URLs | **PASS** | `apps/admin/src/app/dashboard/external-stations/page.tsx`, `GET /stations` | Separated architecturally and legally from managed internal playout |
| **Mushaf & Quran Text** | Medina Mushaf 604 pages, Uthmanic Hafs text | Verified canonical Uthmanic Hafs dataset with 114 Surahs, 6236 Ayahs, 604 Pages | **PASS** | `data/quran/canonical/surahs.json`, `scripts/validate-checksums.sh` | Fail-closed integrity gate strictly rejects altered datasets |
| **Reciters (القراء)** | Multi-reciter directory | Normalized canonical reciter identities with biographies and Riwayat mappings | **PASS** | `GET /reciters`, `apps/admin/src/app/dashboard/reciters/page.tsx`, `RECITER_IDENTITY.md` | Eliminates duplicated names and prevents playback cross-attribution |
| **Audio Player** | Background playback, notification bar controls, seek bar | Integrated AudioHandler, ExoPlayer/just_audio architecture, mini-player bar, bottom sheet | **PASS** | `FadhkurAudioHandler`, `MainActivity.kt` | Background audio & media notification support |
| **Favorites & Playlists** | Save favorite tracks & custom playlists | Curated central playlists and local bookmark persistence | **PASS** | `apps/admin/src/app/dashboard/playlists/page.tsx`, `app/src/main/java/com/example/MainActivity.kt` | Supports offline favoriting |
| **Search (البحث)** | Search by Surah name and number | Instant client-side Arabic normalized search | **PASS** | `MainActivity.kt` (`SearchTextField`) | Supports Arabic diacritics stripping |
| **Offline Audio** | Download Surahs for offline use | Local storage path caching and verification | **PASS** | `data/quran/canonical/`, `MainActivity.kt` | Download streaming directly to disk |
| **Islamic Library & Adhkar** | Daily Morning/Evening Adhkar & fortress of the Muslim | Categorized Adhkar with counters and verified sources | **PASS** | `MainActivity.kt` (Adhkar section) | Pure local execution without network latency |
| **Prayer Times (المواقيت)** | Solar calculation of 5 prayer times | Offline calculation based on Umm Al-Qura without sharing GPS with servers | **PASS** | `GET /home`, `MainActivity.kt` (Prayer Times Bar) | Zero surveillance location privacy |
| **Local Reminders** | Daily Quran reading reminders | Device-scheduled local notifications | **PASS** | `apps/admin/src/app/dashboard/schedules/page.tsx` | Scheduled purely client-side |
| **Push Notifications** | Remote announcements and broadcast alerts | Privacy-first opt-in notification architecture with token masking; server-side FCM HTTP v1 dispatch exclusively via the `notifications` Edge Function using Supabase Secrets (never bundled in client) | **PASS** | `apps/admin/src/app/dashboard/notifications/page.tsx`, `supabase/functions/notifications/` | Full token exposure avoided; immediate revocation capability; 11 FCM topics opt-in only |
| **Video Channels (قنوات الفيديو)** | Curated video channel directory with HLS/YouTube/MP4 playback | `VideoChannelRepository` with empty-state on missing table, admin section `/dashboard/video-channels`; playback deps (`video_player`, `chewie`, `youtube_player_flutter`) added to `pubspec.yaml` | **PARTIAL** | `supabase/migrations/20260830040700_video_channels_table.sql`, `apps/admin/src/app/dashboard/video-channels/page.tsx`, `apps/mobile/lib/features/video/presentation/video_player_screen.dart` | ⚠️ Migration موجود كملف لكن **لم يُطبَّق** على قاعدة البيانات — يتطلب تطبيقًا يدويًا من مالك قاعدة البيانات بعد تثبيت الجيل المرجعي (انظر `docs/DEPLOYMENT.md`) |
| **Remote Config** | Dynamic feature flags & emergency notice | DB-backed sanitized runtime config with schema validation | **PASS** | `GET /runtime-config`, `apps/admin/src/app/dashboard/settings/page.tsx` | Strict anti-code-injection policy |
| **Maintenance / Forced Update** | Kill-switch and update prompt | `min_supported_version` & `maintenance` object enforcement | **PASS** | `GET /runtime-config`, `apps/admin/src/app/dashboard/settings/page.tsx` | Prevents obsolete apps from connecting |
| **Localization (اللغات)** | Arabic primary, English secondary | RTL-first layout with Arabic primary strings and English transliterations | **PASS** | `res/values/strings.xml`, `data/quran/canonical/surahs.json` | No clipped text; culturally appropriate typography |
| **Themes (المظهر)** | Dark & Light modes | Sovereign M3 palette with Pearl White (`#F8F6F1`) & Deep Night (`#0E1726`) | **PASS** | `apps/admin/src/app/dashboard/layout.tsx`, `MainActivity.kt` | High contrast and eye-safe reading |
| **Admin Web (لوحة الإدارة)** | Full operational back-office | 14 specialized sections in Next.js/TypeScript covering all domains (incl. `/dashboard/video-channels`) | **PASS** | `apps/admin/src/app/dashboard/` | Full RBAC, Audit Trail, and dark/light switching |
| **RBAC & Security Audit** | Admin roles & audit logging | Strict Supabase RLS, `app.permissions`, append-only audit trail | **PASS** | `supabase/migrations/20260201000000_fadhkur_core_schemas.sql`, `app.audit_logs` | No secrets leaked in logs |
| **Audio Processing Worker** | Normalization & loudness measurement | Node/TypeScript worker claiming jobs with fencing, EBU R128 (-16 LUFS) | **PASS** | `services/audio-worker/src/index.ts` | Calculates SHA-256 for verified integrity |
| **Continuous Integration (CI/CD)**| Automated tests, linting, release pipelines | 7 decoupled GitHub Actions workflows for deterministic and release builds | **PASS** | `.github/workflows/` | Clean separation of PR CI and live acceptance |

## جولة الإكمال — 2026-09-23 (توحيد + مصحف ملوّن + إثراء)

| الميزة | التنفيذ | الحالة | الدليل | ملاحظات |
| :--- | :--- | :--- | :--- | :--- |
| **توحيد الـ migrations** | ملفات idempotent جديدة `20260830040900` → `20260830041500` تُقارب أي حالة DB إلى schema موحّد (RBAC seed، حقول campaigns، `app.video_channels`، سياسات القراءة، pg_cron، جداول إدارية) دون تعديل الملفات القديمة | **PASS** (كملفات) | `supabase/migrations/20260830040900*.sql` … `20260830041500*.sql`، `docs/MIGRATION_CONSOLIDATION.md` | ⚠️ التطبيق على قاعدة البيانات يدوي من مالك DB — لا وصول من هذه البيئة |
| **المصحف المدني الملوّن** | نسخة تجويد ملوّنة 604 صفحات، مُتحقق منها فعلياً (HTTP 200 + image/jpeg + 604 ملفات بلا فجوات) | **PASS** | `docs/MUSHAF_SOURCES.md`، `apps/mobile/lib/features/quran/domain/mushaf_edition.dart` | المصدر: QuranHub/quran-pages-images (easyquran hafs-tajweed) |
| **المصحف المدني العادي** | نسخة عادية 604 صفحات مُتحقق منها بنفس المنهجية | **PASS** | `docs/MUSHAF_SOURCES.md` | المصدر: QuranHub/quran-pages-images (kfgqpc hafs-wasat) |
| **الخطوط الأربعة** | UthmanicHafs (tarteel CDN ✓) وKFGQPC-Hafs (jsDelivr ✓) بروابط مُتحقق منها + تحميل وكاش على القرص مع fallback لخط النظام؛ QCF وUthmanic-vector بلا مصدر موثّق → ملاحظة صادقة | **PARTIAL** | `apps/mobile/lib/features/quran/data/quran_font_loader.dart`، `docs/MUSHAF_SOURCES.md` | خطّان يعملان فعلياً؛ الآخران fallback أنيق |
| **Force Update حقيقي** | قراءة `min_supported_version`/`min_supported_build` من `app_config` عند الإقلاع (non-blocking)، حوار حاجب Material 3 RTL، ومتابعة صامتة عند تعذّر الشبكة | **PASS** | `apps/mobile/lib/core/services/force_update_service.dart`، `supabase/seed/09_app_config.sql` | القيم المزروعة: `"1.0.64"` / `64` |
| **جدولة الحملات** | `campaign-dispatcher` تعالج `scheduled` المستحقة + pg_cron + توثيق التفعيل | **PASS** (كود) | `supabase/functions/campaign-dispatcher/`، `docs/CAMPAIGN_SCHEDULER.md` | التفعيل يتطلب أسرار Supabase وقت النشر |
| **radio-schedule حقيقي** | قراءة المحطات النشطة + `app.schedules` + `radio.now_playing` مع failover صريح وأخطاء صادقة | **PASS** | `supabase/functions/radio-schedule/index.ts` | لا بيانات تجريبية |
| **صفحات الإدارة الست** | categories/playlists/schedules/audit/devices/identity مربوطة بجداول حقيقية مع حالات صادقة | **PASS** (واجهات) | `apps/admin/src/app/dashboard/{categories,playlists,schedules,audit,devices,identity}/` | ⚠️ خمسة جداول مقيدة بـ service-role — تحتاج سياسة admin-JWT أو وكيل Edge Function (موثق كمتابعة) |
| **المسبحة الإلكترونية** | عدّادات متعددة + HapticFeedback + حفظ محلي — offline بالكامل | **PASS** | `apps/mobile/lib/features/tasbih/` | استبدلت الشاشة القديمة بنسخة محسّنة دون كسر التوجيه |
| **أسماء الله الحسنى** | 99 اسماً ببيانات حقيقية + شرح مختصر + بحث + مفضلة محلية | **PASS** | `apps/mobile/lib/features/allah_names/` | محتوى مدمج حقيقي |
| **حديث اليوم** | الأربعون النووية بتناوب يومي | **PASS** | `apps/mobile/lib/features/daily_hadith/` | نصوص حقيقية مدمجة |
| **متتبع الختمة** | خطة 30 جزءاً على `reading_progress_repository` | **PASS** | `apps/mobile/lib/features/khatma/` | — |
| **التاريخ الهجري** | حساب تقريبي offline بجانب الميلادي مع وسم «تقريبي» | **PASS** | `apps/mobile/lib/core/utils/` | خوارزمية مُصححة (floor division) |
| **مؤقت النوم + السرعة** | مؤقت إيقاف للراديو/الصوت + سرعة التشغيل | **PASS** | `apps/mobile/lib/features/player/presentation/playback_options.dart` | — |
| **بيانات السور** | 114 سورة حقيقية (6236 آية) بدل العينات | **PASS** | `apps/mobile/lib/features/quran/data/surah_metadata.dart` | — |
| **روابط النظام** | `fadhkur://` عبر intent-filter + `routeFromUri` عبر نفس allowlist | **PASS** | `apps/mobile/lib/core/routing/external_deep_link_service.dart`، `AndroidManifest.xml` | Universal Links بالدومين متابعة |
| **كفاءة/جمال** | Skeleton loaders، ودجت EmptyState موحّد، haptics، تدقيق const، كاش `app_config` | **PASS** | `apps/mobile/lib/core/widgets/{skeleton,empty_state}.dart` | — |
| **حقوق النشر** | النص الحرفي «حقوق الطبع والنشر خاصة لمعتز العلقمي - تعز - اليمن 2026م» في شاشة حول التطبيق وREADME | **PASS** | `about_and_sources_screen.dart`، `README.md` | — |

**معلّق على مالك قاعدة البيانات**: تطبيق migrations `40900`→`41500`، ضبط أسرار Quran Foundation وFCM، سياسة وصول الإدارة (admin-JWT/وكيل)، رابط صور المصحف المرخّصة في `app_config` إن أُريد غير المصادر المفتوحة.

## جولة Offline-first — 2026-09-23 (التطبيق يعمل من الصندوق دون باك-إند)

| الميزة | التنفيذ | الحالة | الدليل | ملاحظات |
| :--- | :--- | :--- | :--- | :--- |
| **إعدادات القرآن — fallback مضمّن** | `QuranRuntimeConfig.fallback()` من السجلات المضمّنة؛ `load()` لا ترمي أبدًا عند أول تشغيل — شاشة المصحف (وضع الصور) تعمل بالكامل دون باك-إند | **PASS** | `apps/mobile/lib/features/quran/data/quran_config_repository.dart`، `test/features/quran/quran_config_fallback_test.dart` | أُصلح أيضًا خطأ ترجمة حقيقي: `MushafEditionRegistry.parseList` كان مفقودًا (الكود المُسلَّم لم يُترجم) |
| **نص الآيات — مصدر عام مباشر** | `QuranPublicTextSource`: خريطة الصفحات من `api.quran.com/api/v4` (بلا مفتاح) + نص حفص العثماني من `fawazahmed0/quran-api` عبر jsDelivr (بلا مفتاح)؛ السور تُخزَّن على القرص ملفًا لكل سورة | **PASS** | `apps/mobile/lib/features/quran/data/quran_public_text_source.dart`، `test/features/quran/quran_public_text_fallback_test.dart` | بُنية الـ JSON مُتحققة فعليًا بـ HTTP قبل الاعتماد عليها |
| **lookupPage — fallback عام** | `api.quran.com/api/v4/verses/by_key/{c}:{v}?fields=page_number` (بلا مفتاح، مُتحقق) | **PASS** | `quran_api_repository.dart` | — |
| **التفسير — رسالة صادقة** | لا مصدر عام موثّق لنصوص التفاسير → رسالة صادقة مع زر إعادة (ممنوع نص مُختلَق)؛ فهرس التفاسير يعود للقائمة المضمّنة | **PASS** | `quran_api_repository.dart` | يحتاج نشر `quran-yutla-api` الجديدة للمحتوى |
| **الراديو — 4 محطات مضمّنة** | الحصري/المنشاوي/المعيقلي/الشاطري عبر `backup.qurango.net` — كل رابط مُتحقق (HTTP 200 + audio/mpeg)؛ محطات Supabase الإنتاجية (`production_enabled`) أولًا ثم المضمّنة بلا تكرار | **PASS** | `apps/mobile/lib/features/radio/radio_station.dart`، `test/radio_builtin_fallback_test.dart` | نص الـ empty state حُدّث |
| **بيانات السور للتحميل** | `SurahMetadataService` يسقط للبيانات المضمّنة (114 سورة) عند غياب الشبكة والكاش | **PASS** | `apps/mobile/lib/core/services/quran_download_service.dart` | — |
| **روابط صوت السور** | `server7.mp3quran.net` مُتحققة (HTTP 206 + audio/mpeg) | **PASS** | `recitations_screen.dart` | — |
| **توثيق Offline-first** | مصفوفة: كل ميزة × تعمل دون باك-إند؟ × مصدر الـ fallback + قائمة المصادر المُتحققة | **PASS** | `docs/OFFLINE_FIRST.md` | — |
