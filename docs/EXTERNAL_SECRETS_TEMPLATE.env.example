# Copy to a secure local .env file. Never commit the filled file.
# Public/client-safe values
SUPABASE_URL=https://ttvasynaxkkcwrmvlorg.supabase.co
SUPABASE_PUBLISHABLE_KEY=<copy from Supabase Project Settings > API > Publishable key>
FIREBASE_API_KEY=<copy from Firebase Project settings; client key only>
FIREBASE_ANDROID_APP_ID=1:512347419509:android:2cad141a837c31c6f9193c
FIREBASE_MESSAGING_SENDER_ID=512347419509
FIREBASE_PROJECT_ID=fadhkur-2f78c
FIREBASE_STORAGE_BUCKET=fadhkur-2f78c.firebasestorage.app

# GitHub Actions signing secrets
ANDROID_KEYSTORE_BASE64=<base64 encoded release keystore>
ANDROID_KEY_ALIAS=<keystore alias>
ANDROID_KEY_PASSWORD=<keystore key password>
ANDROID_STORE_PASSWORD=<keystore store password>
FIREBASE_GOOGLE_SERVICES_JSON_BASE64=<base64 encoded google-services.json>

# Supabase Edge Function secrets — keep only in Supabase Secrets, never in the app
FIREBASE_CLIENT_EMAIL=<Firebase Admin SDK service-account client_email>
FIREBASE_PRIVATE_KEY=<Firebase Admin SDK private_key, with newlines preserved or escaped>

# Security: do not put Google passwords, GitHub PATs, Supabase service_role keys,
# or Firebase Admin private keys in this file, GitHub, the APK, or chat messages.
# Rotate any credential previously pasted into chat before using this template.
