# infrastructure/liquidsoap

Dynamic audio routing, scheduling injection, crossfading, and failover engine for **Fadhkur (فذكر)**.
Exposes port `1234` for telnet commands from `services/radio-engine`.
